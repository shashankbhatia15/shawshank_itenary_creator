import React from 'react';

// This component is a placeholder to fix a build error caused by invalid file content.
// It is not used in the application and can be safely deleted.
const LoadPlanModal: React.FC = () => {
  return null;
};

export default LoadPlanModal;
